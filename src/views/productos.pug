extends layout.pug

block title
    title Productos | Sanders Gutiérrez

block content
    h2 Lista de Productos
    each camiseta in camisetas
        article.article
            h2.titulo= `Camiseta ${camiseta.titulo}`
            img(src=`/static/img/${camiseta.imagen}`, width='250')
            p= camiseta.descripcion
            a.comprar(href=`/productos/comprar/${camiseta.titulo}`) Comprar
